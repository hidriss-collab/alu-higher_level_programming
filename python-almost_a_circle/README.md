# Python - Almost a circle

This project implements Base, Rectangle and Square classes with
serialization/deserialization (JSON and CSV) and full unit tests
using the unittest module.

## Usage
Run all tests with:
python3 -m unittest discover tests

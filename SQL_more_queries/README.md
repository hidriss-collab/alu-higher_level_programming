# SQL More Queries

This project covers advanced SQL concepts using MySQL 8.0. It includes creating and managing MySQL users and privileges, creating databases and tables with different constraints, working with primary and foreign keys, writing subqueries and joins, and querying data from multiple related tables. It also demonstrates how to retrieve, filter, sort, and aggregate data from relational databases using SQL.

# python-hello_world

This project covers basic Python scripting and the shell scripting
needed to run Python files.

## 0. Run Python file
`0-run` is a Bash script that runs a Python script whose filename is
stored in the environment variable PYFILE.
